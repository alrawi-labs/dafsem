@model dafsem.Models.ViewModels.AlerModal

<div id="@Model.ModalId" class="modal fade" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content" style="box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3); border-radius: 10px;">
            <div class="modal-body text-center py-5">
                <!-- Spinner -->
                <div class="spinner-border text-info" style="width: 4rem; height: 4rem;" role="status">
                    <span class="visually-hidden">Loading...</span>
                </div>
                <p class="mt-4 fw-bold text-secondary">@Model.Message</p>
            </div>
        </div>
    </div>
</div>

<script>
    function LoadingModalHide(modalId) {
        // $(modalId).removeClass('show');
        // $('body').removeClass('modal-open');
        // $('.modal-backdrop').remove();
        // console.log(modalId)
        // // $('#' + modalId).css("display", "none")
        // console.log("Hidding")
        // console.log("model Id : " + modalId)
        $('#' + modalId).modal('hide')


    }
</script>
